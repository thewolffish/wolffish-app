Two things went wrong in the list store this week and I have put both into `test/store.test.js`: a listener that was registered right after a `once` listener never got its first notification, and after dispatching a `sort` the `prev` state handed to change listeners already shows the sorted order, so our undo feature restores the wrong thing. `npm test` fails. Please fix them properly; the README says what the store guarantees.

When a list has several rows with the same score, paging through it with the cursor drops some of them: page 2 jumps straight past the rows that tied with the last row on page 1. There is a test for it in `test/paginate.test.js` and `npm test` currently fails. Please fix the pagination so every row comes back exactly once no matter how the pages are cut. Keep the cursor format opaque as it is.
